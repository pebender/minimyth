diff -Naur mythtv-master-20191130-a27754a-old/mythtv/programs/mythfrontend/main.cpp mythtv-master-20191130-a27754a-new/mythtv/programs/mythfrontend/main.cpp
--- mythtv-master-20191130-a27754a-old/mythtv/programs/mythfrontend/main.cpp	2019-12-23 22:20:49.114999983 -0800
+++ mythtv-master-20191130-a27754a-new/mythtv/programs/mythfrontend/main.cpp	2019-12-23 22:20:50.387999984 -0800
@@ -2144,6 +2144,18 @@
                 QString("NetworkControl failed to bind to port %1.")
                    .arg(port));
     }
+    else
+    {
+        int port = gCoreContext->GetNumSetting("NetworkControlPort", 6546);
+        networkControl = new NetworkControl();
+        QList<QHostAddress> LocalhostList;
+        LocalhostList.clear();
+        LocalhostList.append(QHostAddress("127.0.0.1"));
+        if (!networkControl->listen(LocalhostList, port))
+            LOG(VB_GENERAL, LOG_ERR,
+                QString("NetworkControl failed to bind to port %1.")
+                   .arg(port));
+    }
 
 #if CONFIG_DARWIN
     GetMythMainWindow()->SetEffectsEnabled(false);
